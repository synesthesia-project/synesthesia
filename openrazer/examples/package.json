{
  "description": "Example scripts using openrazer",
  "license": "MIT",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-clean": "^0.3.2",
    "gulp-sourcemaps": "1.6.0",
    "gulp-tslint": "^8.1.3",
    "gulp-typescript": "^5.0.0",
    "run-sequence": "^1.2.2",
    "tslint": "^5.9.1",
    "typescript": "^3.5.2"
  },
  "scripts": {
    "clean": "gulp clean",
    "build": "gulp",
    "auto-install": "yarn install; git checkout node_modules",
    "test": "node build"
  },
  "dependencies": {
    "openrazer": "^0.0.1"
  }
}
