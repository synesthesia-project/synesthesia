{
  "private": true,
  "scripts": {
    "clean": "yarn workspaces run clean",
    "build": "yarn workspaces run build"
  },
  "workspaces": [
    "core",
    "core/examples/consumer",
    "core/examples/controller",
    "composer"
  ]
}